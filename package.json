{
  "name": "txintegration",
  "version": "0.0.1",
  "description": "Testing integration of RPC Sidecar and TxWrapper.",
  "main": "index.ts",
  "author": "joepetrowski <joe@parity.io>",
  "license": "MIT",
  "scripts": {
    "build": "rimraf lib/ && tsc -p tsconfig.build.json",
    "lint": "tsc --noEmit && eslint . --ext ts"
  },
  "dependencies": {
    "@polkadot/api": "^1.13.0-beta.7",
    "@polkadot/util-crypto": "^2.10.0-beta.1",
    "@substrate/txwrapper": "^1.2.7",
    "ts-node": "^8.10.1",
    "typescript": "^3.8.3"
  }
}
